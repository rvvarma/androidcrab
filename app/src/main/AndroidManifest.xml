<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apptecherzz.apcrabs">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="in.quagga.QT_API_KEY"
            android:value="quagga_api_key" />
        <!-- Splash screen -->
        <activity
            android:name=".splash"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" />
        <activity android:name=".mainscreen" />
        <activity android:name=".OTP" />
        <activity android:label="Profile" android:name=".profil_view" />
        <activity android:label="Farmer" android:name=".form" />
        <activity android:label="Supplier" android:name=".supplier" />
        <activity android:label="Buyer" android:name=".buyerform" />
        <activity android:label="Declaration" android:name=".declaration"></activity>
    </application>

</manifest>